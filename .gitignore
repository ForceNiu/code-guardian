# deps
node_modules/

# next.js
.next/
out/
next-env.d.ts
*.tsbuildinfo

# env
.env
.env.local
.env*.local

# prisma
prisma/*.db

# runtime caches
.cache/

# demo fixture（自带独立 git 历史，不进外层仓库）
fixtures/

# 备份文件（编辑器 / 工具留的旧版副本，不入库）
*.bak
*.backup

# E2E 验证的原始产物（本地跑一次即生成的日志 / 截图 / 结果 dump）——有意不入库。
# 理由：第三方无法独立复现（需本机仓库 + AI 密钥），它们的价值是「确实跑过」
# 而非「可供复验」。结论与逐条断言见 docs/reports/E2E-VERIFICATION-2026-09-16.md，
# 其正文已逐字引用关键日志行；复现方式 `npm run scan <仓库> <baseRef> <headRef>`。
# 附带好处：这些文件含本机绝对路径与内网 IP，不入库即无需长期维护脱敏。
docs/reports/e2e-logs/
